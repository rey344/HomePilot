# Backend API URL (no trailing slash). Only needed if backend runs on a different host/port.
# Local dev: backend runs on 9001, so omit this file and the app will use http://localhost:9001
NEXT_PUBLIC_API_URL=http://localhost:9001
