# Copy to .env and set for local dev or CI.
# Backend reads DATABASE_URL (see backend/app/config.py).

# PostgreSQL (default when running with Docker Compose)
DATABASE_URL=postgresql://homepilot:homepilot@localhost:5432/homepilot

# Optional: use SQLite for tests or local run without Postgres
# DATABASE_URL=sqlite:///:memory:
